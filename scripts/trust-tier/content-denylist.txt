# Genuinely-EXTERNAL identifiers that must never appear in tracked file CONTENT.
# One extended-regex (grep -E) per line; blank lines and #-comments ignored.
#
# Per the 2026-06-14 operator decision, internal identifiers (*.home.arpa,
# *.lab.ropxr.dev, and the cluster's own RFC-1918 ranges 172.16.20.x / .21.x)
# are ACCEPTED in content — they are non-routable, secretless, and already in
# the public mirror, and they are load-bearing in manifests (registry/LiteLLM
# base URLs, LB IPs, runner targets). Do NOT add them here.
#
# This list catches only leaks that are NOT part of the accepted internal
# corpus — things that would be genuinely sensitive or out-of-scope in a
# public cluster repo. Keep it TIGHT to avoid false positives; see README.md.

# Work VLAN (172.16.80.0/24) — a different trust domain (caddy-work etc.);
# nothing in the mini-cluster repo should reference it.
\b172\.16\.8[0-9]\.[0-9]{1,3}\b

# Add genuinely-external sensitive literals below as they come up, e.g.:
#   - real public/WAN IPs of estate edges
#   - third-party real domains that shouldn't be associated with this repo
# (the accepted internal corpus lives in README.md, not here.)
