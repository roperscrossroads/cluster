---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonMetadata:
  labels:
    app.kubernetes.io/name: cluster-health
    app.kubernetes.io/instance: automation

resources:
  - rbac.yaml
  - secrets/litellm-cluster-health-key.sops.yaml
