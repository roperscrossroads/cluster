---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./secret.sops.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  # TRANSIENT: adopt-then-prune of 4 legacy orphan records. Remove in the
  # follow-up commit once external-dns has written the ownership TXT.
  - ./orphan-prune.dnsendpoint.yaml
