---
# Pin: bump tag here on chart upgrade, then re-vendor CRDs alongside
# (see kube-prometheus-stack-crds/ commit message for re-vendor steps).
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: kube-prometheus-stack
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 85.1.3
  url: oci://ghcr.io/prometheus-community/charts/kube-prometheus-stack
