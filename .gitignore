# Secrets
*.pub
*.key
*.decrypted~*.yaml
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.toml
# Kubernetes
kubeconfig
talosconfig
# Misc.
.private/
.venv/
.DS_Store
Thumbs.db

# Rendered Talos node configs (plaintext PKI). Excluded at root level in
# addition to the per-folder .gitignore inside talos/clusterconfig/.
/talos/clusterconfig/

# Local pre-commit cache
.pre-commit-cache/

# Decrypted SOPS output
*.dec.yaml
*.dec.yml

# Local Claude Code tooling state
.claude/
